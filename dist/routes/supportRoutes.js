"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/*import { Router } from 'express';
import * as s from '../controllers/supportController';
import { protect } from '../middlewares/authMiddleware';
const router = Router();
router.post('/', s.createTicket);
router.get('/', protect, s.getTickets);
router.put('/:id', protect, s.updateTicket);
router.delete('/:id', protect, s.deleteTicket);
export default router;
*/ 
